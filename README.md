# ronzz-linux-server-processing-sync

This repository is used to sync files to servers at Ronzz.org from desktops for processing.

## Teleport

- Base location
  ```bash
  cd ronzz-linux-server-processing-sync # base
  ```

- transcription
  ```bash
  cd whisper-transskribo # transcription
  ```